{
  "name": "new-quiz-ready-netlify",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node -e \"console.log('No build needed')\"",
    "check": "node --check netlify/functions/api.mjs && node --check public/app.js"
  },
  "dependencies": {
    "@netlify/blobs": "latest"
  }
}
